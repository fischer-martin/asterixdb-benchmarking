DROP DATAVERSE FENF IF EXISTS;
CREATE DATAVERSE FENF;
USE FENF;

CREATE TYPE FENFType AS {{
    auto_id: uuid
}};


CREATE DATASET FENF(FENFType) PRIMARY KEY auto_id AUTOGENERATED;

LOAD DATASET FENF USING localfs((`path`=`{host}://{path}`),(`format`=`json`));
